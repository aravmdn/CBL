# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
test-results
playwright-report
.playwright-mcp
.env
.env.*
!.env.example
*.local

# Scratch / working dir
tmp/

# Loose teammate handoff artifacts (kept locally, NOT in git).
# Sources were extracted into td/arduino/reference/ and "EngineeringArt CBL/".
/*.zip
/*.jpeg
/*.jpg
/Screenshot*.png
# Teammate TouchDesigner tutorial files (binary, learning artifacts, kept local)
/First tryout*.toe
/Second project*.toe

# TouchDesigner auto-increment backups + crash folders (keep only td/cbl.toe)
td/*.[0-9]*.toe
td/Backup/
*.toe-shm
*.toe-wal
td/*.toe.bak

# TouchDesigner Python: vendored runtime + bytecode are NOT committed.
# Recreate pylibs/ with: pip install -r td/requirements.txt --target td/pylibs
td/pylibs/
td/__pycache__/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
